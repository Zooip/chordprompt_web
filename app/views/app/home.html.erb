<%= javascript_pack_tag 'application' %>
<%= stylesheet_pack_tag 'application' %>



<%= content_tag :div,
                id: "chordprompt-app",
                data: {
                        currentUser: {
                            id: current_user.id,
                            email: current_user.email
                        },
                        jamSession: {
                            id: current_user.jam_sessions.current.id.to_s
                        }
                    }.to_json do %>
<% end %>